[project]
name = "ebay-proj"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Fabian Seidl", email = "fabianseidl@fseidl.eu" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ebay-proj = "ebay_proj:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
